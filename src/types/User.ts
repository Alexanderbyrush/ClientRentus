export interface User {
  id: number;
  name: string;
  email: string;
  phone: string;
  address: string;
  id_documento: string;
  status: string;
  registration_date: string; // ISO format string
}
